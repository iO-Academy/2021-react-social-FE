import Button from '@mui/material/Button';

function SignInButton() {
    return (
        <Button variant="contained" color="greeny" size="large" type="submit" value="Submit">Sign In</Button>
    )
}

export default SignInButton;